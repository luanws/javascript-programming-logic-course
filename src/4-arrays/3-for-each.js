// O for each executa uma função para cada elemento do array.
// Funciona de uma forma muito semelhante ao for of.

const array = [1, 2, 3, 4, 5]
array.forEach(element => console.log(element)) // 1 2 3 4 5