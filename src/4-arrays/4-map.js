// O map retorna um novo array com os resultados da função passada como parâmetro
// O map sempre retorna um array de mesmo tamanho do array original

const array = [1, 2, 3, 4, 5]
array.map(element => element * 2) // [2, 4, 6, 8, 10]