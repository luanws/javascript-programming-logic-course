// A função arrow é uma função anônima com retorno implícito

// Função arrow
const sum = (a, b) => a + b

// Se a função arrow tiver apenas um parâmetro,
// pode-se omitir os parênteses
const sumOne = a => a + 1